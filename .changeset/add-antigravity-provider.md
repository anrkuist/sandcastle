---
"@ai-hero/sandcastle": minor
---

Add support for the `antigravity` agent provider (backing the `agy` CLI tool), allowing users to orchestrate Antigravity agents in isolated sandbox environments.
